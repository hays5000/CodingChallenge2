<%= form_tag unit_path :method => :put do |f| %>


  <div class="field">
    <%= label_tag 'Name: ' %>
    <%= text_field_tag :name, @unit[0]['name'] %>
  </div>

  <div class="actions">
    <%= submit_tag "Update" %>
  </div>
<% end %>
